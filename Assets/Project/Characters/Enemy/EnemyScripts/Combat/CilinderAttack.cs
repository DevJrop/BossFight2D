using UnityEngine;

/*namespace Project.Characters.Enemy.EnemyScripts.Combat
{
    public class CilinderAttack : AttackExecutorBase
    {
        private void ExecuteRotatingRadial()
        {
            float angleStep = 360f / bulletCount;

            for (int i = 0; i < bulletCount; i++)
            {
                float angle = i * angleStep + rotationOffset;
                Vector2 direction = GetDirectionFromAngle(angle);

                SpawnBullet(direction, radialAttack);
            }
        }

        public override void Execute(AttackContext context)
        {
            throw new System.NotImplementedException();
        }
    }
}*/
